/** Claim topics — must match RwaConstants.sol */
export const CT_KYC_APPROVED = 666n;
export const CT_MNFT_ISSUER = 7n;
export const CT_MNFT_REGULATOR = 8n;
export const SCHEME_ECDSA = 1n;

/** Arbitrum One */
export const ARBITRUM_ONE_CHAIN_ID = 42161;
/** Arbitrum Sepolia */
export const ARBITRUM_SEPOLIA_CHAIN_ID = 421614;

/** Default transfer fee allowance for two 10-token transfers at ~1% (MockFeeToken, 18 decimals). */
export const DEFAULT_TRANSFER_FEE_ALLOWANCE = 200_000_000_000_000_000n;

/** Default vault mint amount (100 security tokens, 18 decimals). */
export const DEFAULT_VAULT_MINT_AMOUNT = 100_000_000_000_000_000_000n;
